// Central place for environment / API configuration.
// Override via an Expo `extra` field or env wiring later if staging is needed.
export const API_BASE_URL = 'https://portal.thermalhazardtechnology.com/api';
