{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "jsx": "react-jsx",
    "module": "esnext", // Changed to esnext
    "moduleResolution": "bundler", // Kept as bundler
    "types": ["react-native"]
  }
}
